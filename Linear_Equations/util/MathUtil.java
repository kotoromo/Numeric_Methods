public class MathUitl{
  public static determinant(Matrix A){

    Double det = 0.0;
    
    Matrix cofactor = new Matrix(i, j);
    // Determinant for kxk matrix
    for(int i = 1; i<k; i++){

    }
  }
}
